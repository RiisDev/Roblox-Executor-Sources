#pragma once
#include <Windows.h>
#include <map>

class RetcheckBypass {
public:
	static INT cache(PBYTE function) {
		// function used to cache a protected roblox function, strip the security, then read from that cache
		// snipped (private reasons)
		// bypassing retcheck isn't hard, do it yourself thx
		return 0;
	}
};